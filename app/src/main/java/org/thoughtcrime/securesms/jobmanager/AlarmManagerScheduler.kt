package org.thoughtcrime.securesms.jobmanager

import android.app.Application

class AlarmManagerScheduler(application: Application) : Scheduler {
    override fun schedule(delay: Long, constraints: List<Constraint>) {

    }
}