package org.signal.core.util

class Stopwatch constructor(
    private val title: String,
    private val decimalPlaces: Int = 0,
    private val code: Int = 0,
) {

    fun split(label: String) {

    }

    fun stop(label: String) {

    }
}